spring:
  jpa:
    show-sql: true
    properties:
      format_sql: true
      dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create
  datasource:
    url: ${URL}
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${NAME}
    password: ${PASSWORD}
    application:
    name: jpa
  output:
    ansi:
      enabled: always
